spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/viplavfauzdar/spring-cloud-config
          search-paths:
          - config-server-files
management:
  endpoint:
    restart:
      enabled: true
